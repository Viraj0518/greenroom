// Maintainer bootstrap placeholder — UI (tenzinyeshi-07) owns app/ and should replace this.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "4rem", textAlign: "center" }}>
      <h1>GreenRoom</h1>
      <p>Open-source speaker &amp; content management. Skeleton deploy — UI coming soon.</p>
    </main>
  );
}
